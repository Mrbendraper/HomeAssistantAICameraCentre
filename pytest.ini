[pytest]
testpaths = tests
# put the repo root on sys.path so `custom_components...` imports resolve
# (default prepend-import mode only adds the tests/ dir)
pythonpath = .
asyncio_mode = auto
addopts = -q -p no:cacheprovider
